<h1>Edit a portfolio item</h1>

<%= form_with(model: @briefcase) do |form| %>
  <% if @briefcase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@briefcase.errors.count, "error") %> prohibited this briefcase from being saved:</h2>

      <ul>
      <% @briefcase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :subtitle %>
    <%= form.text_field :subtitle %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>